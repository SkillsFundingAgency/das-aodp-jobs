namespace SFA.DAS.AODP.Data.Entities;

public static class QaaPublicationStatus
{
    public const string PendingNew = nameof(PendingNew);
    public const string PendingChange = nameof(PendingChange);
    public const string Published = nameof(Published);
}
